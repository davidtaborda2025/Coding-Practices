services:
  db:
    image: postgres:15
    container_name: database_test
    restart: always
    environment:
      POSTGRES_USER: administrador_db
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: test_db
    ports:
      - "5432:5432"
    volumes:
      - ./SQL:/docker-entrypoint-initdb.d